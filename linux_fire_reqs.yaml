---
- name: Pre-baking for linux firewall config with ansible
  hosts: "{{ host }}"
  tasks:
  - name: Install firewalld
    ansible.builtin.package:
      name: firewalld
      state: present
    become: yes
